<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    style="@style/Artist.Row"
    >

  <ImageView
      android:id="@+id/avatar"
      android:layout_width="@dimen/avatar_small_width"
      android:layout_height="@dimen/avatar_small_width"
      android:layout_marginRight="@dimen/default_padding"
      />
  <TextView
      android:id="@+id/name"
      android:layout_toEndOf="@id/avatar"
      android:layout_toRightOf="@id/avatar"
      android:layout_marginTop="@dimen/default_padding"
      android:layout_marginBottom="@dimen/default_padding"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Bonodo"
      style="@style/Artist.Name"
      />
  <TextView
      android:id="@+id/genres"
      android:layout_toEndOf="@id/avatar"
      android:layout_toRightOf="@id/avatar"
      android:layout_below="@id/name"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="dance, electronics"
      style="@style/Artist.Genres"
      />

  <TextView
      android:id="@+id/works"
      android:layout_toEndOf="@id/avatar"
      android:layout_toRightOf="@id/avatar"
      android:layout_alignBottom="@id/avatar"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      style="@style/Artist.Works"
      />
</RelativeLayout>